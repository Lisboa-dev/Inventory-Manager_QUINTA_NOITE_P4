model Produto {

  
 // Relation with User
  usuario            User @relation("ProdutosToUser", fields: [usuarioId], references: [id], onDelete: Cascade)
  usuarioId         Int
 // Relation with Lote
  lote             Lote @relation("ProdutosToLote", fields: [loteId], references: [id], onDelete: Cascade)
  loteId            Int


  id                Int      @id @default(autoincrement())
  barCode           String
  nome              String
  descricao         String? 
  preco             Decimal   @db.Decimal(10, 2)
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
}